{
  "comment": "to run dev server in utility container, use 'docker run -e NODE_OPTIONS=--openssl-legacy-provider -dit -p 80:8080 -v ./:/home/node -w /home/node --name catpod-docs --rm node:21-alpine npm docs:dev'",
  "devDependencies": {
    "@vuepress/bundler-vite": "^2.0.0-rc.20",
    "@vuepress/theme-default": "^2.0.0-rc.91",
    "sass-embedded": "^1.86.3",
    "vuepress": "^2.0.0-rc.20"
  },
  "engines": {
    "node": ">=22",
    "pnpm": ">=10"
  },
  "scripts": {
    "docs:dev": "vuepress dev docs",
    "docs:build": "vuepress build docs"
  }
}
