{
  "comment": "to run dev server in utility container, use 'docker run -e NODE_OPTIONS=--openssl-legacy-provider -dit -p 80:8080 -v ./:/home/node -w /home/node --name catpod-docs --rm node:22-alpine npm docs:dev'",
  "dependencies": {
    "@vuepress/bundler-vite": "^2.0.0-rc.21",
    "@vuepress/theme-default": "^2.0.0-rc.91",
    "sass-embedded": "^1.87.0",
    "vuepress": "2.0.0-rc.21"
  },
  "scripts": {
    "docs:dev": "vuepress dev docs",
    "docs:build": "vuepress build docs"
  }
}
